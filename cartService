GET /cart
function getCart(req, res){

	input : user_id
	if cart is not present:
	return cart is not present
    if cart is present:
	return cart
}

POST /cart
function createCart(req, res){
	input: user_id, items => item_ids[], quantities []
	create a new cart for that paticular user with specific item_ids and quantity 
	return
	if(item is not present){
		return error
	}


}

PUT /cart/:item_id
function editCart(req, res){
	input => cart_id, item_id, quantity
	if(cart_id is not present){
		return error
	}
	else if(item is not present){
		return error
	}
	else{
		add specific item to the cart with the specific quantity
	}
}

Delete cart/:item_id
function deleteItem(req, res){
	input => cart_id, item_id
	if(cart_id or item_id is not present){
		return error
	}
	else{
		delete that item from the cart
	}
}

